var canvas,ctx;function Linker(canvas_id){this.id=canvas_id;this.getID=function(){return this.id}}function Frame(width,height){this.width=width;this.height=height;this.getWidth=function(){return this.width};this.getHeight=function(){return this.height}}function bindLinker(linker){if(!(linker instanceof Linker)){console.log("Please supply a valid linker to bind.");return}canvas=document.getElementById(linker.getID());ctx=canvas.getContext('2d')}function bindFrame(frame){if(!(frame instanceof Frame)){console.log("Please supply a valid frame to bind.");return}canvas.width=frame.getWidth();canvas.height=frame.getHeight()}function clear(){}function drawRectangle(r,g,b,x,y,width,height){var color="#"+((1<<24)+(r<<16)+(g<<8)+b).toString(16).slice(1);ctx.fillStyle=color;ctx.fillRect(x,y,width,height)}
